import { Game } from './core/Game.js';

// Wait for the DOM to be fully loaded before starting the game
window.addEventListener('DOMContentLoaded', () => {
    const game = new Game();
    console.log('Thirst Sea - Survival Prototype Initialized');
});
