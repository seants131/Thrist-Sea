Prompt Ngày 2: Chu kỳ Ngày/Đêm & Tài nguyên cạn kiệt
Role: Senior Game Developer
Project: "Thirst Sea" (Three.js + Vite)
Context: We already have the basic scaffolding (State, Game loop, Hydration system, Economy system).
Task: Implement a Day/Night cycle, dynamic lighting, and the "Outer Rim" resource depletion mechanic.

Please provide the complete updated code for the following files. Use ES Modules.

1. Update src/core/State.js

Add new properties: time: 0 (ticks/seconds), isNight: false, and coalPiles: [] (array to store resource objects).

2. Create NEW file src/entities/Resource.js

Export a function createCoalPile(x, z).

Return a Three.js Mesh using IcosahedronGeometry (radius 2, detail 0 to make it look low-poly/rough).

Material color should be dark grey/black (0x222222).

3. Update src/core/Game.js

Lighting: Replace basic lights with a dynamic DirectionalLight (Sun).

Initialization: In the setup phase, generate 5 Coal Piles. Place them at a fixed radius = 30 from the center, spaced evenly in a circle (use polar coordinates to calculate x and z). Add them to State.coalPiles and the Scene.

Tick Loop (every 1 sec): >   - Increment State.time++.

Calculate Day/Night: A full day is 60 ticks. If State.time % 60 > 30, set State.isNight = true, else false.

Animate Sun: Change the DirectionalLight color and intensity based on day/night. (e.g., bright white/yellow for day, dim blue/purple for night).

4. Update src/systems/Economy.js

Consumption Logic: Check State.isNight. If false (Day), consume 1 Coal. If true (Night), consume 2 Coal (purifier works harder).

Depletion Mechanic: Every 300 ticks (5 minutes), trigger a depletion event:

Check if State.coalPiles.length > 0.

Remove the last pile from the State.coalPiles array.

You must receive the scene as a parameter to remove the pile's mesh: scene.remove(pileMesh).

(Optional) console.log("A coal pile has been depleted!").

Update HTML UI to show current Time (Tick count) and Phase (Day/Night).

Output: Output the exact code blocks for State.js, Resource.js, Game.js, and Economy.js. Ensure the code is robust and handles errors (like running out of coal piles).