3. Chỉ dẫn cho AI để sửa lỗi "Hình hộp thẳng"
Bạn hãy gửi yêu cầu này cho Gemini để nó nâng cấp file src/entities/Building.js:

Task: Replace BoxGeometry with a custom "Curved Trapezoid" geometry for buildings in Thirst Sea.

Requirements:

In src/entities/Building.js, instead of THREE.BoxGeometry, use THREE.CylinderGeometry to create the building mesh.

Parameters for the Geometry:

radiusTop and radiusBottom should be calculated based on the Ring's distance from the center (e.g., innerRadius and outerRadius).

height: Set a fixed height for the building (e.g., 2 units).

radialSegments: Set to a low number like 8 to keep it optimized but enough to show the curve.

thetaLength: This is the key. Calculate it so the building width perfectly matches the slot width of that specific ring: thetaLength = (2 * Math.PI) / numSlots.

Rotation: Ensure the mesh is rotated correctly so the "curved" sides follow the rings of the grid.

Result: The buildings should now look like segments of a ring, fitting perfectly side-by-side without gaps, just like in Frostpunk.

Output: Provide the updated createBuilding function.