Prompt 2.2: Hệ thống Thu thập (Gathering System)
Mục tiêu: Làm cho các mỏ than (Coal Piles) có ý nghĩa. Người chơi phải xây "Trạm thu thập" gần mỏ than để tự động cộng thêm Than mỗi giây.

Sau khi Prompt 2.1 chạy tốt, hãy copy đoạn này:

Task: Implement the "Gathering System" for Thirst Sea. Players need to build a "Gathering Post" near Coal Piles to mine them.

1. Update index.html & State.js:

Add a new UI button: [Build: Gathering Post (Cost: 20 Coal)].

Update State.buildMode to handle 'GATHERING_POST'.

2. Update src/entities/Building.js:

Modify createBuilding(type, x, z). If type === 'HOUSE', return a standard Cube. If type === 'GATHERING_POST', return a different shape (e.g., a short, wide box with a dark brown color 0x5C4033).

3. Update src/systems/Economy.js (The Gathering Logic):

In the update() tick: Loop through all buildings in State.buildings.

If a building is a 'GATHERING_POST', check its distance to all State.coalPiles.

If a Coal Pile is within a working radius (e.g., radius < 10), that Gathering Post mines +2 Coal per second.

Add this mined coal to State.coal BEFORE deducting the Purifier's consumption.

Output: Provide the updated Building.js and Economy.js. Explain how the distance check is calculated.